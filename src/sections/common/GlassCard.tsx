// src/sections/common/GlassCard.tsx
// Section-level re-export — use this for glass cards inside section files
// to keep imports consistent within the sections layer.
// The actual component lives in @/components/ui/GlassCard.

export { default } from "@/components/ui/GlassCard";
